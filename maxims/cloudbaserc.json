{
  "envId": "{{env.ENV_ID}}",
  "version": "2.0",
  "framework": {
    "name": "maxims",
    "plugins": {
      "container": {
        "use": "@cloudbase/framework-plugin-container",
        "inputs": {
          "serviceName": "maxims",
          "servicePath": "/maxims",
          "containerPort": 8080
        }
      }
    }
  }
}